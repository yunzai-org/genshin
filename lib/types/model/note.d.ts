import base from './base.js';
export default class Note extends base {
    constructor(e: any);
    static get(e: any): Promise<any>;
    getData(): Promise<any>;
    noteSr(res: any): any;
    noteData(res: any): any;
}
